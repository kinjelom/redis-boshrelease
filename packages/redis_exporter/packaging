#!/usr/bin/env bash

set -eux

source meta-info/blobs-versions.env

mkdir -p "${BOSH_INSTALL_TARGET}/bin"
tar zxf "redis_exporter/redis_exporter-v${REDIS_EXPORTER_VERSION}.tar.gz" --strip 1 -C "${BOSH_INSTALL_TARGET}/bin"